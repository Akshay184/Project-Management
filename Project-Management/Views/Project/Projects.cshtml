
@foreach (var project in ViewBag.Projects)
{
    
    @Html.ActionLink((string)project.ProjectName,"Message","Dashboard",new {ProjectId = project.ProjectId,ProjectName = project.ProjectName},false);
    <br/>
}